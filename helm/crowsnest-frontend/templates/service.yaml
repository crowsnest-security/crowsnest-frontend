apiVersion: v1
kind: Service
metadata:
  name: {{ include "crowsnest-frontend.fullname" . }}
  labels:
    {{- include "crowsnest-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crowsnest-frontend.selectorLabels" . | nindent 4 }}
